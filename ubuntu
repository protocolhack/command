sudo apt update && sudo apt install -y jadx binutils && \
jadx -d output app.apk && \
(
echo "==== CLASS NAMES ====" &&
grep -r "class " output/ | head -n 50 &&
echo -e "\n==== METHOD NAMES ====" &&
grep -r "void " output/ | head -n 50 &&
echo -e "\n==== PACKAGE STRUCTURE ====" &&
ls output/sources/ | head -n 50 &&
echo -e "\n==== STRINGS (HTTP/API/KEY) ====" &&
strings app.apk | grep -Ei "http|api|token|key" | head -n 50
) > apk_obfuscation_poc.txt